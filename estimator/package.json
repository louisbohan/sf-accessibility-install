{
  "name": "accessibility-install-estimator",
  "version": "1.0.0",
  "description": "Accessibility install estimator — ballpark range wizard with wholesale-protected backend",
  "type": "module",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "test:watch": "node --experimental-vm-modules node_modules/.bin/jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "transform": {},
    "extensionsToTreatAsEsm": []
  }
}
